//password
//  и еще одно поле для особенных role / moreWords
export default class User {
  constructor({
    _id,
    name,
    googleId,
    email,
    avatar,
    read_today_num,
    read_today_arr,
    daily_reading_goal,
    finished_texts,
    moreWords,
    date
  }) {}
}
